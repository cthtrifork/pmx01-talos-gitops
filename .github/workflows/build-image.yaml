name: Publish to Github
on:
  pull_request: 
    branches: 
      - main
    paths:
      - images/github-actions-runner/*
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'image tag prefix'
        default: 'rc'
        required: true

permissions:
  contents: read

env:
    RUNNER_VERSION: "2.336.0" # renovate: datasource=github-releases depName=actions/runner

jobs:
    publish-images:
        outputs:
            image_url: ${{ steps.slsa.outputs.image_url }}
            image_digest: ${{ steps.slsa.outputs.image_digest }}
        runs-on: ubuntu-latest
        permissions:
            contents: read
            id-token: write # for creating OIDC tokens for signing.
            packages: write # for pushing and signing container images.
        strategy:
            matrix:
                image:
                    - name: github-actions-runner
                      containerfile: images/github-actions-runner/Dockerfile
                      context: images/github-actions-runner
                      registryurl: ghcr.io/${{ github.repository }}/github-actions-runner
        steps:
            - name: Checkout repository
              uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

            - name: Prepare
              id: prep
              run: |
                VERSION="${DEFAULT_TAG:-rc}-${GITHUB_SHA::8}"
                if [[ $GITHUB_REF == refs/tags/* ]]; then
                  VERSION=${GITHUB_REF/refs\/tags\//}
                fi
                echo "BUILD_DATE=$(date -u +'%Y-%m-%dT%H:%M:%SZ')" >> $GITHUB_OUTPUT
                echo "VERSION=${VERSION}" >> $GITHUB_OUTPUT
              env:
                DEFAULT_TAG: ${{ github.event.inputs.tag }}

            - name: Set up QEMU
              uses: docker/setup-qemu-action@96fe6ef7f33517b61c61be40b68a1882f3264fb8 # v4.2.0

            - name: Set up Docker Buildx
              uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.3.0

            - name: Generate images meta
              id: meta
              uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302 # v6.2.0
              with:
                images: |
                  ${{ matrix.image.registryurl }}
                tags: |
                  type=raw,value=${{ steps.prep.outputs.VERSION }}

            - name: Log in to the Container registry
              uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.6.0
              with:
                  registry: ghcr.io
                  username: ${{ github.repository_owner }}
                  password: ${{ secrets.GITHUB_TOKEN }}

            - name: Build and push Docker image
              uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.3.0
              id: build-push
              with:
                  context: ./${{ matrix.image.context }}
                  file: ./${{ matrix.image.containerfile }}
                  platforms: linux/amd64
                  push: ${{ github.event_name != 'pull_request' }}
                  tags: ${{ steps.meta.outputs.tags }}
                  labels: ${{ steps.meta.outputs.labels }}
                  cache-from: type=gha
                  cache-to: type=gha,mode=max
                  sbom: ${{ github.event_name != 'pull_request' }}
                  provenance: ${{ github.event_name != 'pull_request' }}
                  build-args: |
                      DOCKER_METADATA_OUTPUT_VERSION
                      RUNNER_VERSION=${{ env.RUNNER_VERSION }}
                      VERSION=${{ steps.prep.outputs.VERSION }}
       
            - uses: sigstore/cosign-installer@6f9f17788090df1f26f669e9d70d6ae9567deba6 # v4.1.2
            - name: Sign images
              if: ${{ github.event_name != 'pull_request' }}
              env:
                COSIGN_EXPERIMENTAL: 1
              run: |
                cosign sign --yes ${{ matrix.image.registryurl }}@${{ steps.build-push.outputs.digest }}

            - name: Generate SLSA metadata
              id: slsa
              if: ${{ github.event_name != 'pull_request' }}
              run: |
                image_url=${{ matrix.image.registryurl }}:${{ steps.prep.outputs.VERSION }}
                echo "image_url=$image_url" >> $GITHUB_OUTPUT
            
                image_digest=${{ steps.build-push.outputs.digest }}
                echo "image_digest=$image_digest" >> $GITHUB_OUTPUT

    ghcr-provenance:
        needs: [publish-images]
        if: startsWith(github.ref, 'refs/tags/v') && github.event_name != 'pull_request'
        permissions:
            actions: read # for detecting the Github Actions environment.
            id-token: write # for creating OIDC tokens for signing.
            packages: write # for uploading attestations.
        uses: slsa-framework/slsa-github-generator/.github/workflows/generator_container_slsa3.yml@f7dd8c54c2067bafc12ca7a55595d5ee9b75204a # v2.1.0
        with:
            image: ${{ needs.publish-images.outputs.image_url }}
            digest: ${{ needs.publish-images.outputs.image_digest }}
            registry-username: ${{ github.actor }}
        secrets:
            registry-password: ${{ secrets.GITHUB_TOKEN }}

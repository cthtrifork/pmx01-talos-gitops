{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Annotate YAML file with JSON schemas",
      "type": "process",
      "command": "${workspaceFolder}/scripts/annotate-yaml.py",
      "args": [
        "${file}"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    },
  ]
}

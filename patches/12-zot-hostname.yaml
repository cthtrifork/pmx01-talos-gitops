machine:
  network:
    extraHostEntries:
      - ip: 192.168.1.240
        aliases:
          - zot.lan
  registries:
    mirrors:
      zot.lan:5000:
        endpoints:
          - http://zot.lan:5000
    config:
      zot.lan:5000:
        tls:
          insecureSkipVerify: true
